# Why Lindy AI Should Hire Chinmay

A targeted, single-page pitch site built for [Lindy AI](https://lindy.ai) — inspired by the "Why You Should Hire Me" format.

## What This Is

A personalized one-pager that highlights why Chinmay is the right Data Engineer for Lindy AI, anchored around a self-initiated project: the **Lindy Credit Intelligence Dashboard** — a full analytics pipeline built by analyzing real user pain points from Trustpilot reviews.

## Live Site

> Replace with your GitHub Pages URL after deployment.

## Deploy to GitHub Pages

1. Create a new repository on GitHub
2. Push this project:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
   git push -u origin main
   ```
3. Go to **Settings → Pages**
4. Under **Source**, select **Deploy from a branch**
5. Choose `main` branch and `/ (root)` folder
6. Click **Save**
7. Your site will be live at `https://YOUR_USERNAME.github.io/YOUR_REPO/`

## Before Going Live

Replace the placeholder `#` links in the footer with your actual URLs:

- **Live Dashboard** — link to your Streamlit dashboard
- **GitHub** — link to your GitHub profile or project repo
- **LinkedIn** — link to your LinkedIn profile

## Tech

- Pure HTML + CSS + vanilla JS — no frameworks, no build step
- Google Fonts: DM Serif Display + DM Sans
- Dark mode with `localStorage` persistence
- Fully responsive (mobile, tablet, desktop)

## Local Development

Open `index.html` in any browser. No server required.
